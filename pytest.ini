[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    -v
    --tb=short
    --strict-markers
    -p no:warnings

markers =
    unit:        mark a test as a unit test
    integration: mark a test as an integration test (uses real fixture files)
    slow:        mark a test as slow (skipped by default in CI fast mode)
